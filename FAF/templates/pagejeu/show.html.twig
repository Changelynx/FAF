{% extends 'base.html.twig' %}

{% block title pagejeu.title %}

{% block body %}
<article>
    <h1>{{ pagejeu.title }}</h1>
    <p>'{{ pagejeu.description }}</p>
</article>

    <a href ="{{ path('app_home') }}" >Go Back</a>
{% endblock %}